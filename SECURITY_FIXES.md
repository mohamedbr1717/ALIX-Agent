# ملخص الإصلاحات الأمنية الحرجة — ALIX-Agent

## 1) تجاوز سياسة الأوامر عبر تنفيذ Python (حرج)
**الملف:** `core/policy.py` — `Policy.command_allowed()`

كان بالإمكان تشغيل `python <script>.py` عبر `run_command` حتى لو كانت
قابلية `run_python` معطّلة افتراضيًا (`capabilities["run_python"] = False`).
بما أن `write_file` لا يفحص محتوى الملف، كان أي طرف يقدر يكتب سكربت
Python عشوائي وينفذه عبر `run_command`، متجاوزًا بذلك كل قيود الأوامر
لأن مفسّر Python نفسه غير معزول.

**الإصلاح:** أضفنا فحص `capability_allowed("run_python")` داخل
`command_allowed()` نفسها، بحيث تشغيل Python عبر أي مسار (سواء
`run_command` أو `run_python`) يمرّ من نفس المفتاح المركزي، المعطّل
افتراضيًا.

تم تحديث `test_policy_coverage_v8.py` و`test_generated_policy.py`
و`test_reliability_executor.py` لتعكس هذا السلوك (تفعيل القابلية
صراحة في الاختبارات التي تحتاج تنفيذ Python فعليًا).

## 2) `shell=True` في `tools/system.py`
كود غير مستخدم حاليًا في مسار التنفيذ الحقيقي، لكنه خطر كامن. تم
استبداله بتحليل الأمر إلى argv عبر `policy.parse_command()` وتنفيذه
بدون shell (`shell=False`)، بنفس أسلوب `core/executor.py`.

## 3) عزل الكود القديم غير المستخدم
`agent.py` في جذر المشروع (نسخة قديمة، غير مستوردة من أي مكان،
تحتوي `shell=True` وتُدخل الأوامر مباشرة) نُقلت إلى
`deprecated/agent.py.DO_NOT_USE` مع ملف `deprecated/README.md`
يوضح سبب عدم استخدامها.

## 4) خادم MCP يوهم بتنفيذ كود لا يُنفَّذ فعليًا
أداة `alix_execute_code` كانت توصف بأنها "تنفذ الأكواد الآمنة"، بينما
`ALIXv41Agent.execute_task()` لا تستدعي `exec()` إطلاقًا — فقط تصنّف
الكود. أعدنا تسميتها `alix_check_code` مع وصف صادق (فحص ثابت فقط، لا
تنفيذ)، وأضفنا توثيقًا صريحًا في `policy_watcher.py` و
`alix_v4_1_unified.py` يوضح أن اجتياز الفحص لا يعني أن الكود آمن
للتنفيذ (قابل للالتفاف عبر تقنيات معروفة مثل الوصول لـ dunder attributes
عبر `str.format` بدل عقد AST مباشرة). كما أضفنا فرض `max_code_length`
من `policy.json` (كان معرّفًا وغير مستخدم) ومعالجة `SyntaxError` بشكل
صريح.

## 5) توثيق ملفات الـ "sandbox" الوهمية
`wasm_sandbox.py`, `redteam_sandbox.py`, `dynamic_policy_sandbox.py`
غير مستخدمة في مسار التنفيذ الحقيقي (فقط في اختباراتها الخاصة). أُضيفت
تحذيرات صريحة أعلى كل ملف توضح ذلك وتشرح لماذا نهج AST denylist ليس
حماية حقيقية.

## النتيجة
150/150 اختبارًا ناجحًا (باستثناء فشل استيراد واحد غير متعلق بالأمان
بسبب عدم تثبيت حزمة `openai` في بيئة الاختبار — كان موجودًا قبل أي
تعديل).

## الجولة الثانية — core/agent.py و core/memory.py

### 6) `core/registry.py`: بوابة capability كانت مفقودة تمامًا
`ToolRegistry.execute()` (كود غير مستخدم حاليًا في main.py، لكنه نقطة
امتداد محتملة) كان يتحقق من `tool_allowed()` فقط، بدون
`capability_allowed()`. أُضيف الفحص بحيث لو استُخدم هذا الـ registry
مستقبلاً، لن يعيد فتح ثغرة تجاوز `run_python` التي أُصلحت سابقًا.

### 7) توحيد فلترة `list_files`
`tools/filesystem.py.FileSystemTools.list_files` كان يُخفي فقط
الملفات التي تبدأ بنقطة، بخلاف النسخة الفعلية في
`core/agent.py._execute_tool_body` التي تفلتر أيضًا عبر
`policy.is_sensitive_path()` (تُخفي `credentials.json`, `id_rsa`, إلخ
حتى بدون نقطة بالبداية). أُضيف نفس الفحص في `tools/filesystem.py`
لتوحيد السلوك.

### 8) `core/memory.py`: `add_history` كانت تخزّن نصًا خامًا بلا فلترة أو موافقة
خلافًا لـ `remember_fact` (يتطلب موافقة المستخدم)، فإن كل رسالة
مستخدم/مساعد تُكتب تلقائيًا لملف `memory.json` بدون أي بوابة. أُضيفت
مجموعة أنماط تعرف على الأسرار الشائعة (مفاتيح AWS، مفاتيح بصيغة
`sk-...`، رموز GitHub، كتل PEM للمفاتيح الخاصة، Bearer/JWT tokens،
وتعبيرات `password=`/`api_key=`...) ويتم استبدالها بـ
`[REDACTED_...]` قبل أي حفظ على القرص — في `add_history`،
`add_fact`، و`add_preference` عبر `_sanitize_text` المشترك.

هذه فلترة "قائمة على النمط" وليست ضمانًا كاملاً — أي سر لا يطابق أحد
الأنماط المعروفة سيمر دون تغيير.

### 9) صلاحيات ملفات الذاكرة
`memory.json` و`memory.json.bak` أصبحا يُقيَّدان إلى `0600` (المالك
فقط) بعد كل عملية حفظ، بدل الاعتماد على صلاحيات النظام الافتراضية.

### 10) حماية شاشة الموافقة من حقن الطرفية (Terminal Spoofing)
`confirm_tool()` في `core/agent.py` كان يطبع قيم `arguments` كما هي.
أُضيفت `_safe_display()` التي:
- تزيل أكواد ANSI escape (يمكن استخدامها لإخفاء/تزييف ما يظهر في
  الطرفية قبل موافقة المستخدم).
- تزيل محارف Unicode bidi control (يمكن استخدامها لعكس اتجاه عرض
  النص وخداع القارئ).
- تحذف بقية محارف التحكم غير القابلة للطباعة.
- تقصّ العرض عند 2000 محرف.

### النتيجة
150/150 اختبارًا ناجحًا (نفس الاستثناء البيئي السابق بخصوص حزمة
`openai` غير المثبتة، غير متعلق بالتعديلات).

## الجولة الثالثة — عزل حقيقي على مستوى نظام التشغيل لـ run_python

البيئة الفعلية: **Termux على أندرويد (بدون root)**. هذا يستبعد
bubblewrap و nsjail (يحتاجان unprivileged user namespaces، معطّلة
غالبًا في نوى أندرويد الرسمية). الأداة الواقعية المتاحة بدون root
هي **`proot`** (معتمدة رسميًا في مستودعات Termux، تعمل عبر ptrace).

### الجديد: `core/sandbox.py` — `ProotSandbox`
عزل حقيقي (مُنفَّذ من النواة/ptrace، وليس مجرد allowlist للأوامر):
- **نظام ملفات**: السكربت لا يرى إلا مجلد العمل و`$HOME` وهمي فارغ
  ومسارات Termux الأساسية. لا وصول لأي مسار آخر حتى بمسارات مطلقة.
- **حدود موارد حقيقية** (`setrlimit`، بدون root): زمن CPU، الذاكرة،
  أقصى عدد عمليات (يمنع fork bombs)، أقصى ملفات مفتوحة، أقصى حجم
  مخرجات.
- **`PR_SET_NO_NEW_PRIVS`** لمنع اكتساب صلاحيات جديدة.
- **Fail-closed صريح**: إن لم يكن `proot` مثبتًا، `run_python` يرفض
  التنفيذ بخطأ واضح بدل التراجع الصامت لتشغيل غير معزول.

### حدود مُعلنة بصراحة (غير مخفية)
- **لا عزل شبكة حقيقي**: يحتاج root (iptables) أو netns، وكلاهما غير
  متاح بدون root على أندرويد. السكربت المُنفَّذ **يقدر يفتح اتصال
  شبكي صادر**. الوحيد المضاف هو حجب `/etc/resolv.conf` (يمنع فقط حل
  الأسماء عبر DNS، لا الاتصال المباشر بـ IP) — خط دفاع جزئي وضعيف،
  ذُكر ذلك بوضوح في توثيق الكود.
- **لا فلترة seccomp**: تم استبعادها عمدًا بدل تقديم فلتر هش غير
  مُختبر (مكتبات seccomp لبايثون غير متوفرة بشكل موثوق كحزمة Termux).

### التغييرات المرتبطة
- `core/executor.py.run_python()` يستخدم الآن `ProotSandbox` بدل
  `subprocess.run([sys.executable, ...])` المباشر.
- `SafeExecutor.__init__` يقبل `sandbox_cls` قابلة للحقن، لاختبار
  منطق التنفيذ بدون الحاجة لتثبيت `proot` فعليًا في بيئة الاختبار.
- `test_sandbox.py` (جديد): يختبر سلوك fail-closed دائمًا (يعمل بدون
  proot)، بالإضافة لاختبارات تكاملية حقيقية (تنفيذ فعلي، منع القراءة
  خارج workspace، قتل حلقة CPU لانهائية، صد fork bomb) — هذه
  الاختبارات تُخطَّى تلقائيًا عند غياب `proot` وتُشغَّل فعليًا عند
  توفره.
- `.github/workflows/ci.yml`: أضيفت خطوة `apt-get install -y proot`
  حتى تُختبر آلية العزل الحقيقية على كل push، لا فقط مسار الرفض.

### النتيجة
156/156 اختبارًا (4 منها اختبارات تكامل حقيقية للعزل، تُخطَّى محليًا
هنا لغياب `proot` وستعمل فعليًا في CI/على Termux الحقيقي).

### توصية صريحة قبل الاعتماد الكامل
هذه الاختبارات التكاملية الأربعة في `test_sandbox.py` **لم تُشغَّل
فعليًا بعد على جهاز Termux حقيقي** من طرفي (لا تتوفر بيئة أندرويد في
أداة التنفيذ المستخدمة هنا). قبل الاعتماد الكامل على `ProotSandbox`
في الإنتاج:
1. شغّل `pkg install proot` على جهازك.
2. شغّل `python -m unittest test_sandbox.py -v` وتأكد أن الاختبارات
   الأربعة تنجح فعليًا (لا "skipped").
3. جرّب يدويًا: `run_python` على سكربت يحاول قراءة ملف خارج
   workspace (مثل `~/.termux/`) وتأكد أنه يفشل كما هو متوقع.

## تحديث بعد الاختبار الفعلي على Termux حقيقي (python3.14)

نتيجة أول تشغيل فعلي: **3 من 4 اختبارات تكامل نجحت مباشرة** (منع
القراءة خارج workspace، قتل حلقة CPU لانهائية، صد fork bomb) — أي أن
عزل proot وحدود setrlimit تعمل فعليًا على الجهاز، لا نظريًا فقط.

الاختبار الرابع (`test_basic_execution_works`) فشل بخطأ
`mmap failed: Out of memory` (return_code=-6). السبب: القيمة
الافتراضية لـ `max_memory_mb` كانت 256، وهذا أقل مما يحتاجه مفسّر
python3.14 على هذا الجهاز فقط للإقلاع (RLIMIT_AS يقيس الذاكرة
الافتراضية المحجوزة لا الفعلية المستخدمة، ومفسّرات Python الحديثة
تحجز مساحة عناوين كبيرة عند الإقلاع بغض النظر عن الاستخدام الفعلي).

**الإصلاح:** رُفعت القيمة الافتراضية إلى 768MB، ووُثِّق هذا القيد
صراحة في `core/sandbox.py` (مع تنبيه أن رفع القيمة أكثر قد يكون
مطلوبًا حسب الجهاز).

**نقطة منهجية مهمة تم تداركها:** بما أن اختبار "منع القراءة خارج
workspace" كان ينجح فقط بالتحقق من `ok == False`، كان من الممكن أن
ينجح **للسبب الخاطئ** (انهيار السكربت بسبب نفاد الذاكرة الافتراضية،
لا لأن proot منع الوصول فعليًا). أُضيف تحقق صريح إضافي:
`return_code != -6` و`"mmap failed" not in stderr` و`"Error" in
stderr`، للتأكد أن الرفض ناتج عن خطأ Python طبيعي (مثل
`FileNotFoundError`) بسبب إخفاء proot للمسار، لا عن انهيار عرضي غير
متعلق بالعزل.

### الخطوة التالية المطلوبة منك
أعد تشغيل `python -m unittest test_sandbox.py -v` بعد سحب هذا
التحديث، وتأكد أن الاختبارات الأربعة تنجح جميعًا الآن، وأن اختبار
منع القراءة خارج workspace ينجح للسبب الصحيح (لا SIGABRT).
## الجولة الرابعة — D2: حقن leet-speak (كان مقبولًا كـresidual، أُغلق الآن جزئيًا)

**الخطر:** المهاجم يموّه عبارات الحقن بأسلوب l33t (مثال:
`1gn0r3 4ll pr3v10us 1nstruct10ns`) — تبقى مقروءة للنموذج (خاصة Qwen
الأضعف في اتباع تعليمات الأمان) بينما تفلت من مطابقة العبارات الحرفية.

**الإصلاح (`core/prompt_guard.py`):** `scan()` تفحص الآن — إضافة للمرور
العادي — نسخًا مطوّأة leet من النص بتبديل 1:1 لكل حرف (المواضع
محفوظة، فالإبلاغ والحجب يقعان على النص الأصلي دائمًا). متغيّران
للحرف الغامض `1` (مرة `i` ومرة `l`) يغطّيان `pr3v10us` و`a11`-style.
**الطي للكشف فقط ولا يعيد كتابة المخرجات أبدًا** — وهذا ما يحمي
سلاسل الإصدارات (`3.5.1`) وأسماء النماذج (`Qwen3.5-4B`) وعناوين IP
من أي تشويه.

**الضمانات المختبرة (`test_leet_guard.py`, 13 اختبارًا):** كشف وحجب
فعلي لخمس عائلات leet، صلاحية المواضع على النص الأصلي، عدم إعادة
كتابة المحتوى السليم، وجناح false positives صريح (إصدارات، IP،
كود، أسعار، عربية بأرقام، `i18n`/`a11y`/`k8s`).

**متبقٍ معلن:** خلط متعمّد لـ`1` (بعضها `i` وبعضها `l` في كلمات
مختلفة من نفس العبارة) يفلت من المتغيّرين المتّسقين، وleet العربي
(Arabizi/Franco) غير مطوّأ.
